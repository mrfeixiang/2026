# InkScape AI — Backend (V0.1)
# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# Image processing (V0.1 works with ONLY this — no GPU, no API key required)
Pillow==11.1.0
numpy==2.2.1

# Optional AI providers (only used if configured; import is lazy)
# openai==1.59.6
# httpx==0.28.1
